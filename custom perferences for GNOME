### Custom shortkeys which I prefer
Super + A = Close the Window
Super + B = Open the Browser		//Firefox
Super + W = Open the Terminal		//Alacritty
Super + Z = Minimize all Window
Super + D = Open file manager
Super + R = Record screen
Super + 1 = Workspace 1
Super + 2 = Workspace 2
Super + > = Move window to left workspace
Super + < = Move window to right workspace
Shift + Ctrl + Super + P = Shutdown

### Custom commands which I use to trigger as shortcuts

nautilus starred:///			// will open "Starred folder" 
nautilus /<path>/<of>/<folder>		// will open desired folder		
