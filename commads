To disable Trackpad or Tocuhscreen, others, we can do this typing:
xinput list                       //To see ID number
xinput disable <ID number>        //For example, "xinput disable 10" will disable my touch pad.

To list all system and user Gnome extensions:
ls -d -1 /usr/share/gnome-shell/extensions/* ~/.local/share/gnome-shell/extensions/* 

To start any service 
sudo service mysql stop		//mysql is one example

Disabling startup service
sudo systemctl disable apache2
or
sudo update-rc.d apache2 disable

To brute force stop services from launching at boot-time:
sudo systemctl mask snapd.service

To stop services that launch in startup
sudo update-rc.d -f windscribe-cli remove

To run GUI app as sudo (Don't run, unless u know what it is) 
pkexec

To install .deb file:
sudo dpkg -i DearLord.deb

Remove repo:		
sudo add-apt-repository -r ppa:<ppa to remove>

Installing .Tar file after extration
./configure && make install
-or- (if not compiling as root)
./configure --prefix=$HOME && make && make install

Hashes of file mismatch
sudo rm -rf /var/lib/apt/lists/partial
sudo apt-get update -o Acquire::CompressionTypes::Order::=gz

Run shell script
chmod +x /path/to/yourscript.sh
./yourscript.sh

Start xamp server:
cd /opt/lampp
sudo ./manager-linux-x64.run

To start or stop Xammp server
sudo /opt/lampp/lampp <start or stop> 

To install app like Grails or other apps from SDK, type
sdk install <package-name>
sdk current                 // to see apps installed from sdk

Resource temporarily unavailable solution:
sudo rm -rf /var/lib/apt/lists/* 
sudo rm /var/cache/apt/archives/lock
or
ps aux | grep -i apt
sudo kill -9 <process id>

Create launcher icon to run app
cat eclipse.desktop
[Desktop Entry]
Type=Application
Name=Eclipse
Exec=<path of execution file which launch app>/eclipse
Icon=<path of icon>/icon.png
Terminal=False
                      //Now save the eclipse.desktop file in /usr/share/applications/

To install .bin extension software
chmod +x ./software-name

Change java enviroment variable:
export JAVA_HOME=/usr/lib/jvm/java-1.8.0-openjdk-amd64     //for java 8
export JAVA_HOME=/usr/lib/jvm/java-1.11.0-openjdk-amd64    //for java 11
export JAVA_HOME=/usr/lib/jvm/default-java      //for default selected java

Git setup: 
sudo add-apt-repository ppa:git-core/ppa  
apt update; apt install git 
git config --global user.name "PranavBhattarai"
git config --global user.email "Prabesh432@gmail.com"

Fix Broken Install
sudo dpkg --configure -a
sudo apt-get install -f

I removed internal wifi kernel from loading by adding
"blacklist iwlwifi" line in /etc/modprobe.d/blacklist.conf
//iwlwifi is my wifi adpter kernel.
